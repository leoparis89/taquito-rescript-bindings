{
  "name": "taquito-bindings",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "res:build": "rescript build",
    "res:watch": "rescript build -w",
    "res:clean": "rescript clean -with-deps",
    "test": "rescript build && retest lib/js/tests/*.bs.js"
  },
  "devDependencies": {
    "@ryyppy/rescript-promise": "^2.1.0",
    "rescript": "^10.0.1",
    "rescript-test": "^4.0.0"
  },
  "peerDependencies": {
    "@taquito/signer": "^13.0.1",
    "@taquito/taquito": "^13.0.1",
    "@taquito/utils": "^13.0.1"
  }
}
